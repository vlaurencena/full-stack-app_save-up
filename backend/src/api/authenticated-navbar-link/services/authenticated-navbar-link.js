'use strict';

/**
 * authenticated-navbar-link service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::authenticated-navbar-link.authenticated-navbar-link');
